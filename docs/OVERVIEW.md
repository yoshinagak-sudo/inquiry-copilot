# inquiry-copilot — サービス概要

> **問い合わせメール対応を、人間の判断を残したまま 5 分の 1 に圧縮する。**
> AI が下書き、人がワンクリックで送信。送ったやり取りからナレッジが自動で育つ。

🔗 デモ: **http://localhost:3001**（ローカル稼働中、まだ未デプロイ）

---

## 解決する課題

- **問い合わせ対応に時間が溶ける**：1件ごとに過去メールを掘り、似た案件の返信を探し、テンプレを切り貼り。慣れた担当ほど属人化する。
- **答えの揺れと、抜け漏れ**：人によって回答が違う／古い情報のまま返してしまう。「前にどう答えたか」が組織に残らない。
- **ナレッジが追いつかない**：FAQ や対応マニュアルは、書いた瞬間から古くなる。実際の問い合わせから自動で育つ仕組みが要る。

---

## 受信箱

![受信箱（デスクトップ）](screenshots/desktop-inbox.png)

舞台ファーム宛の問い合わせを **未着手 / 草案あり / 送信済み** の 3 ステータスでひと目に。
カテゴリ（商品・配送・ふるさと納税・クレーム・採用 など）を色付きバッジで分類し、各メールに **AI 草案の信頼度** がそのまま並びます。
担当者は信頼度の高いものから順に処理できる構成です。

---

## 主な機能

### 1. 受信メールから AI が返信草案を即座に生成

![詳細・草案編集（デスクトップ）](screenshots/desktop-inquiry-detail.png)

メールを開いた瞬間に Claude が登録ナレッジから関連情報を引き、返信文を組み立てます。
草案には **どのナレッジを引用したか** と **AI 自己評価の信頼度（高 / 中 / 低）** が必ず添えられ、根拠の裏取りなしに送ることはできません。
そのままテキストエリアで自由に編集でき、気に入らなければ「再生成」ボタンで何度でもやり直せます。

### 2. 30 秒のキャンセル可能な「ワンクリック送信」

![送信完了画面](screenshots/desktop-inquiry-sent.png)

送信ボタンを押すと **30 秒のカウントダウン** が始まります。誤送信に気づいたらいつでもキャンセル可。
送信後の画面では、AI 草案と人間が実際に送った最終文面の **差分率（diffRatio）** を自動算出し、後述のナレッジ自動更新の判定材料に使います。

### 3. ナレッジ管理（FAQ）

![ナレッジ管理（デスクトップ）](screenshots/desktop-knowledge.png)

商品ライン・配送条件・ふるさと納税の納期・採用情報といった全 FAQ を Markdown で一元管理。
各ナレッジには **何回引用されたか / 最後に使われたのはいつか** が自動記録され、古びたナレッジを発見しやすくしています。
新規追加・編集・削除はモーダルから即時反映、AI 草案にも次回からそのまま反映されます。

### 4. 問い合わせから自動でナレッジ候補を提案

![ナレッジ候補レビュー（デスクトップ）](screenshots/desktop-candidates.png)

人間が AI 草案を **大きく書き換えて送信** したケースを検知すると、その編集差分を解析して「これは新しいナレッジにすべきでは？」と候補を自動生成します。
**毒入り学習を防ぐため、自動でナレッジに反映させることはしません。** 必ず候補レビュー画面で人間が承認 / 却下を判断し、承認したものだけが正式 FAQ に昇格します。
保留中・承認済み・却下済みの 3 タブで進捗管理できます。

---

## モバイル画面

![受信箱（モバイル）](screenshots/mobile-inbox.png)
![草案編集（モバイル）](screenshots/mobile-inquiry-detail.png)

外出先・現場からスマホで触れます。受信箱はカード形式に切り替わり、信頼度・カテゴリ・ステータスが指で確認できる密度に。
草案編集と送信もモバイルで完結し、移動中の隙間時間に「届いた問い合わせを 1 件流す」運用を想定しています。

---

## ナレッジ自動更新ループ

![ナレッジ管理（モバイル）](screenshots/mobile-knowledge.png)
![候補レビュー（モバイル）](screenshots/mobile-candidates.png)

このプロダクトの中核は「対応するほど、組織の知恵が貯まる」ループです。

```
問い合わせ受信
   ↓
AI が既存ナレッジから草案生成（引用元と信頼度を明示）
   ↓
人間がレビュー・編集して送信
   ↓
編集量が大きい返信を検知 → ナレッジ候補を自動生成
   ↓
人間が候補を承認 → 正式 FAQ に昇格
   ↓
次回以降の AI 草案に即反映
```

**完全自動更新は実装していません。** ナレッジ更新は必ず人間承認ゲートを通る設計です。

---

## 触り方（デモガイド）

1. **`/`（受信箱）** を開く。10 件のサンプル問い合わせ（ササニシキの注文 / ふるさと納税の遅延 / クレーム / 採用 / 業務用見積 ほか）が並びます。
2. 任意の問い合わせをクリック。AI が登録ナレッジを引いて草案を組み立てます（初回のみ生成に数秒）。
3. 引用ナレッジを折りたたみで確認しつつ、テキストエリアで草案を自由に編集。
4. **「送信」ボタン** を押すと 30 秒のカウントダウン。途中でキャンセル可、放置で自動送信。
5. 草案を大きく書き換えて送信した場合、**`/knowledge/candidates`** に新規ナレッジ候補が自動で出現。
6. 候補を編集・承認すると、**`/knowledge`** の正式 FAQ に「候補昇格」のピル付きで現れます。
7. 同じ系統の問い合わせを再度開くと、新しく承認したナレッジが草案に反映されているのが確認できます。

> **デモモード** で動作中のため、メールの実送信はモック化されています（DB に送信済みフラグが立つだけで、実メールは発信されません）。

---

## 技術構成

| 項目 | 採用技術 |
|---|---|
| フロントエンド | Next.js 16.2.4（App Router、Turbopack） + React 19 + Tailwind CSS v4 |
| バックエンド | Next.js Route Handlers（TypeScript） |
| AI | Anthropic Claude Sonnet 4.6（API キー未設定時はナレッジ bigram マッチによるモック草案にフォールバック） |
| データベース | SQLite（Prisma 7 + better-sqlite3 ドライバ） ※本番化時は PostgreSQL + pgvector を想定 |
| ナレッジ検索 | デモ規模（〜20件）はプロンプトに全件詰め込み、本番は pgvector による意味検索に切替前提 |
| 受信 | デモはシードデータでモック化（本番は Gmail API + Pub/Sub watch を想定） |
| 送信 | デモはモック送信（本番は Gmail API messages.send + 30 秒取消キュー） |
| 認証 | デモは未実装（本番は NextAuth + Google ドメイン制限を想定） |
